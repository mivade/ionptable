\documentclass[12pt]{article}

% TODO: Note that it is only 9Be+
% TODO: Add hyperfine splitting for S_1/2
% TODO: Add fine structure splitting for P states
% TODO: Label linewidth

\usepackage{../levels}

\begin{document}

\begin{tikzpicture}[scale=1]
  % Level centers
  \input{../nodes}

  % Levels
  \draw[level] (S12) -- +(-\lwidth,0) node[left] {$2^2S_{1/2}$} --
    +(\lwidth,0);  
  \draw[level] (P12) -- +(-\lwidth,0) node[left] {$2^2P_{1/2}$} --
    +(\lwidth,0);
  \draw[level] (P32) -- +(-\lwidth,0) node[left] {$2^2P_{3/2}$} --
    +(\lwidth,0);

  % Transitions
  \draw[transition,blue] ($(S12)-(-.4,-\dy)$) -- ($(P12)-(-.2,\dy)$)
    node[tlabel] {313.1 nm};
  \draw[transition,cyan] ($(S12)-(.4,-\dy)$) -- ($(P32)-(.2,\dy)$)
    node[tlabel] {313.0 nm};
\end{tikzpicture}

\end{document}

%%% Local Variables: 
%%% mode: tex-pdf
%%% TeX-master: t
%%% End: 
